import styled from 'styled-components'

export const BackToCharactersWrapper = styled.button`
  font-size: 16px;
  font-weight: bold;
  outline: none;
  margin: 12px 0;

  svg {
    margin-right: 10px;
  }
`
